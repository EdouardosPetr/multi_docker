/* 

Anytime the worker sees a new index added going to pull that index and then calculate the appropriate Fibonacci value for it

*/

module.exports = {
	redisHost: process.env.REDIS_HOST,
	redisPort: process.env.REDIS_PORT,
};